#!/usr/bin/env bash

set -euo pipefail

repo_root="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")/.." && pwd)"

echo "Refreshing the installed Rollbar plugin and skill..."
"${repo_root}/install.sh"
echo "Orb resume complete."
